# This file is kept for compatibility with pip-based workflows.
# The canonical dependency source is pyproject.toml (managed by uv).
# Regenerate with: uv pip compile pyproject.toml -o requirements.txt
litellm>=1.83.0
pydantic>=2.12.5
python-dotenv>=1.2.2
streamlit>=1.56.0
